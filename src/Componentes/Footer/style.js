import styled from "styled-components";

export const FooterStyle = styled.footer`
    text-align: center;
    color: var(--cor-azulEscuro);
    font-size: 1.25rem ;
    font-weight: 300;
    padding: 2.5rem 0;
`